package org.cloudvision.trading.bot.account;

/**
 * Position side (direction)
 */
public enum PositionSide {
    LONG,   // Buy position (profit when price goes up)
    SHORT   // Sell position (profit when price goes down) - for future use
}

